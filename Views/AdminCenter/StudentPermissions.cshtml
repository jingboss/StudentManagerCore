@model List<Admin>
@{
    ViewData["Title"] = "批量学生权限管理";
    var role = User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value ?? "";
    var isAdmin = role.Trim() == "管理员";
}

@if (!isAdmin)
{
    <div class="alert alert-danger">您没有权限访问此页面</div>
    return;
}

<div class="d-flex align-items-center justify-content-between mb-3">
    <h4 class="mb-0">
        <i class="bi bi-people-fill me-2"></i>批量学生权限管理
        <span class="badge bg-secondary ms-2">共 @Model.Count 人</span>
    </h4>
    <div>
        <a asp-action="TeacherPermissions" class="btn btn-outline-info btn-sm me-2">
            <i class="bi bi-person-gear me-1"></i> 切换到教职工权限
        </a>
        <button type="button" class="btn btn-success" id="btnSaveAll">
            <i class="bi bi-check2-all me-1"></i> 保存全部修改
        </button>
    </div>
</div>

<div class="alert alert-info d-flex align-items-center">
    <i class="bi bi-info-circle-fill me-2"></i>
    设置班主任对<strong>学生</strong>的操作权限。管理员拥有所有权限，不在此页面显示。
</div>

<div class="table-responsive">
    <table class="table table-bordered table-hover align-middle text-center" id="permTable" style="font-size:0.85rem;">
        <thead class="table-warning">
            <tr>
                <th style="min-width:80px;">姓名</th>
                <th style="min-width:60px;">
                    学生→编辑<br/>
                    <input type="checkbox" class="master-check" data-col="1" title="全选/取消" />
                </th>
                <th style="min-width:60px;">
                    学生→删除<br/>
                    <input type="checkbox" class="master-check" data-col="2" title="全选/取消" />
                </th>
                <th style="min-width:60px;">
                    学生→添加<br/>
                    <input type="checkbox" class="master-check" data-col="3" title="全选/取消" />
                </th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count == 0)
            {
                <tr>
                    <td colspan="4" class="text-center text-muted py-4">暂无班主任/教师用户</td>
                </tr>
            }
            @foreach (var teacher in Model)
            {
                var perms = (teacher.Permissions ?? "").Split(',', StringSplitOptions.RemoveEmptyEntries);
                <tr data-id="@teacher.AdminID">
                    <td class="text-start fw-bold">@teacher.RealName</td>
                    <td><input type="checkbox" class="perm-check" data-perm="student_edit" @(perms.Contains("student_edit") ? "checked" : "") /></td>
                    <td><input type="checkbox" class="perm-check" data-perm="student_delete" @(perms.Contains("student_delete") ? "checked" : "") /></td>
                    <td><input type="checkbox" class="perm-check" data-perm="student_add" @(perms.Contains("student_add") ? "checked" : "") /></td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).on('change', '.master-check', function () {
            var colIndex = parseInt($(this).data('col'));
            var isChecked = $(this).prop('checked');
            $('#permTable tbody tr').each(function () {
                $(this).find('td:eq(' + colIndex + ') input.perm-check').prop('checked', isChecked);
            });
        });

        $('#btnSaveAll').on('click', function () {
            var btn = $(this);
            btn.prop('disabled', true).html('<span class="spinner-border spinner-border-sm me-1"></span>保存中...');
            var updates = [];
            $('#permTable tbody tr').each(function () {
                var id = parseInt($(this).data('id'));
                if (!id) return;
                var cells = $(this).find('td');
                updates.push({
                    id: id,
                    editStudent: $(cells[1]).find('input').prop('checked'),
                    deleteStudent: $(cells[2]).find('input').prop('checked'),
                    addStudent: $(cells[3]).find('input').prop('checked')
                });
            });
            $.ajax({
                url: '@Url.Action("BatchUpdateStudentPermissions")',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(updates),
                headers: { 'RequestVerificationToken': $('input[name="__RequestVerificationToken"]').val() },
                success: function (res) {
                    if (res.success) alert('✅ ' + res.message);
                    else alert('❌ ' + (res.message || '保存失败'));
                    btn.prop('disabled', false).html('<i class="bi bi-check2-all me-1"></i> 保存全部修改');
                },
                error: function () {
                    alert('网络错误，请重试');
                    btn.prop('disabled', false).html('<i class="bi bi-check2-all me-1"></i> 保存全部修改');
                }
            });
        });
    </script>
}
