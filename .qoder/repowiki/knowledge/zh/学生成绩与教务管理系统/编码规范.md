- 控制器中的敏感操作（如登录、密码修改）均通过 `PasswordHelper` 进行 PBKDF2 哈希验证，并兼容旧版明文密码。
- 所有涉及用户身份的控制器动作均依赖 Cookie 认证中间件，并通过 `ClaimTypes.Role` 进行细粒度的角色权限控制。
- 数据库实体映射在 `AppDbContext` 中通过 Fluent API 集中配置，明确指定列名、类型及外键约束关系。